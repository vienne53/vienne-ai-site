# 复制为 .env（勿提交 Git）
# Windows 记事本另存时务必选「所有文件」、文件名填 .env —— 否则会保存成 .env.txt，代理读不到。

# ━━━ 你这种：控制台 curl 是 /api/v3/responses ━━━
# API Key = 控制台「API Key」那一串（Bearer 后面那个），不是 AK/SK 里的 Secret Base64
OPENCLAW_API_KEY=粘贴你的_API_Key
# 与 curl 里完全一致，例如：
OPENCLAW_CHAT_URL=https://ark.cn-beijing.volces.com/api/v3/responses
# 与 curl 里 -d 的 model 一致，例如：
OPENCLAW_MODEL=doubao-seed-1-8-251228
# 可显式写（也可不写，只要 URL 带 /responses 会自动识别）：
OPENCLAW_MODE=responses

# 可选：人设
# OPENCLAW_SYSTEM_PROMPT=你是 Bella…
# 可选：访客问「怎么联系你」时用的固定事实（每轮请求都会带上，无需训练模型）
# OPENCLAW_OWNER_FACTS=本人Vienne。电话13331522033。微信Vienne71。邮箱yingnan24@yeah.net。
# 或多行：复制 server/owner-facts.example.txt 为 server/owner-facts.txt（已 gitignore）
# CHAT_PROXY_PORT=8790

# ━━━ 若控制台是 /api/v3/chat/completions（messages）━━━
# OPENCLAW_CHAT_URL=https://ark.cn-beijing.volces.com/api/v3/chat/completions
# OPENCLAW_MODE=doubao
# OPENCLAW_MODEL=ep-xxxx

# ━━━ 自定义网关只收 {"message":""} ━━━
# OPENCLAW_MODE=simple

# ━━━ 部署到 Vercel（在网页用「AI 分身」）━━━
# 不要把 .env 提交到 Git。在 Vercel：Project → Settings → Environment Variables
# 逐项添加与本文件相同的 OPENCLAW_API_KEY、OPENCLAW_CHAT_URL、OPENCLAW_MODEL、OPENCLAW_MODE 等；
# Production / Preview 按需勾选，保存后 Redeploy。
# 部署完成后浏览器打开：https://你的项目.vercel.app/api/health
#   configured: true 且 modelSet: true 再测站内对话。
# 说明：线上页面（非 localhost）会向同域 /api/chat 发请求；api/chat.js 已随仓库部署。
# Hobby 套餐单次函数执行时间有上限，若上游很慢超时，可升级套餐或缩短模型输出（OPENCLAW_MAX_TOKENS）。
